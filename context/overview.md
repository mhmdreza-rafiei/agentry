# Overview

`comprehensive-skills` is an Agent Skills package. Install via `npx skills add mhmdreza-rafiei/comprehensive-skills`.

Shipped skills live in `skills/<name>/`. First skill: `enhance-prompt` (portable prompt rewriter; no path/stack injection).
